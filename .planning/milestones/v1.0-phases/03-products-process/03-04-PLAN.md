---
phase: 03-products-process
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Hero.tsx
  - src/components/ui/SampleRequestForm.tsx
autonomous: false
gap_closure: true
requirements:
  - PROD-01
  - PROD-07

must_haves:
  truths:
    - "Hero trust strip logos are visually readable — Indian Oil emblem is recognisable, Santosh logo is not squished"
    - "Submitting the sample request form without Name, Address, or Phone is blocked with visible error messages"
    - "Filling all required fields and submitting shows the success state"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "Trust strip with correctly-sized logo Images"
    - path: "src/components/ui/SampleRequestForm.tsx"
      provides: "Client-side validation with errors state and error message JSX"
  key_links:
    - from: "SampleRequestForm handleSubmit"
      to: "errors state"
      via: "validation checks before setSubmitted(true)"
      pattern: "if.*!form\\.name|errors\\.name"
---

<objective>
Fix two UAT failures in existing components: (1) hero trust strip logos rendered too small to be readable, (2) sample request form allows submission without required fields.

Purpose: Unblock UAT tests 4 and 14 — both are marked major severity.
Output: Hero.tsx with corrected logo dimensions; SampleRequestForm.tsx with validation guards and inline error messages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hero trust strip logo sizes</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
Read node_modules/next/dist/docs/ before editing (per AGENTS.md). Open src/components/sections/Hero.tsx.

Find the two Image tags in the trust strip (lines ~213-243):
- Indian Oil Image: currently width=24 height=24 — change to width=40 height=40 so the circular emblem is readable
- Santosh Image: currently width=20 height=20 with an 838x1100 portrait SVG — the logo's natural aspect ratio is ~0.76 wide : 1 tall. Change to width=30 height=40 so it renders at a visible size. Keep the existing `className="object-contain brightness-0 invert"` and the surrounding pill containers unchanged.

No other changes to Hero.tsx — only the width= and height= attributes on those two Image tags.
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. Indian Oil Image is width=40 height=40; Santosh Image is width=30 height=40. Text labels beside each logo remain. No layout regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Add required-field validation to SampleRequestForm</name>
  <files>src/components/ui/SampleRequestForm.tsx</files>
  <action>
Read node_modules/next/dist/docs/ before editing (per AGENTS.md). Open src/components/ui/SampleRequestForm.tsx.

Make the following changes — no others:

1. Add an errors state after the existing form state:
   ```typescript
   const [errors, setErrors] = useState<{ name?: string; address?: string; phone?: string }>({});
   ```

2. Replace the handleSubmit body with validation guards before setSubmitted(true):
   ```typescript
   const handleSubmit = (e: React.FormEvent) => {
     e.preventDefault();
     const newErrors: { name?: string; address?: string; phone?: string } = {};
     if (!form.name.trim()) newErrors.name = "Full name is required";
     if (!form.address.trim()) newErrors.address = "Delivery address is required";
     if (!form.phone.trim()) newErrors.phone = "Phone number is required";
     if (Object.keys(newErrors).length > 0) {
       setErrors(newErrors);
       return;
     }
     setErrors({});
     // API call happens in plan 03-05
     setSubmitted(true);
   };
   ```

3. After each required field's `<input>` tag, add an error message paragraph:
   - After the Name input: `{errors.name && <p className="mt-1 text-xs text-red-400" style={{ fontFamily: "'Barlow', sans-serif" }}>{errors.name}</p>}`
   - After the Address input: `{errors.address && <p className="mt-1 text-xs text-red-400" style={{ fontFamily: "'Barlow', sans-serif" }}>{errors.address}</p>}`
   - After the Phone input: `{errors.phone && <p className="mt-1 text-xs text-red-400" style={{ fontFamily: "'Barlow', sans-serif" }}>{errors.phone}</p>}`

Keep `noValidate` on the `<form>` element — our JS validation replaces browser validation intentionally. Keep all other form fields, styling, and the WhatsApp success state unchanged.
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. SampleRequestForm has errors useState, validation logic in handleSubmit that returns early with error messages when name/address/phone are empty, and inline red error text beneath each required field.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify logo sizes and form validation</name>
  <files>src/components/sections/Hero.tsx, src/components/ui/SampleRequestForm.tsx</files>
  <action>Human verification of Tasks 1 and 2. See how-to-verify steps below.</action>
  <what-built>Hero logo size fix + sample request form validation</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. Hero trust strip: scroll to the bottom of the hero. The Indian Oil and Santosh pill badges should show visible logos — not just text. The Indian Oil circular emblem should be recognisable (40px). The Santosh logo should be portrait-oriented and visible (30x40px) alongside the text label.
3. Navigate to http://localhost:3000/sample-request
4. Click "Send Sample Request" without filling anything in — you should see red error messages below Name, Address, and Phone fields. The success state should NOT appear.
5. Fill in Name, Address, and Phone, then submit — the success state with "Sample request received" should appear.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — awaiting human approval"</automated>
  </verify>
  <done>User confirms logos are visible and form validation blocks empty submission.</done>
  <resume-signal>Type "approved" or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with no TypeScript errors
- Hero.tsx: Indian Oil Image width=40 height=40; Santosh Image width=30 height=40
- SampleRequestForm.tsx: errors useState present; handleSubmit guards name/address/phone before setSubmitted; error JSX beneath each required field
</verification>

<success_criteria>
UAT test 4 passes: trust strip logos are visually readable at their new sizes.
UAT test 14 passes (validation half): form refuses to submit without required fields and displays inline errors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-products-process/03-04-SUMMARY.md` following the summary template.
</output>
