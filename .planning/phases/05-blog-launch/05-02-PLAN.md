---
phase: 05-blog-launch
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/sections/PostCard.tsx
  - src/components/blog/PortableTextRenderer.tsx
  - src/app/insights/page.tsx
  - src/app/insights/[slug]/page.tsx
  - src/components/sections/LatestInsights.tsx
  - src/components/layout/Nav.tsx
  - src/components/layout/Footer.tsx
autonomous: true
requirements: [BLOG-01, BLOG-02]

must_haves:
  truths:
    - "/insights page renders a grid of post cards fetched from Sanity"
    - "/insights/[slug] page renders a single post with Portable Text body in dark ink-900 theme"
    - "LatestInsights homepage section fetches 3 most recent posts from Sanity and uses dark ink-900 theme"
    - "Nav and Footer include /insights link"
    - "Zero posts state renders empty message on /insights page"
  artifacts:
    - path: "src/components/sections/PostCard.tsx"
      provides: "Reusable post card component for listing and LatestInsights"
      exports: ["PostCard"]
    - path: "src/components/blog/PortableTextRenderer.tsx"
      provides: "Custom Portable Text renderer with site design tokens"
      exports: ["PortableTextRenderer"]
    - path: "src/app/insights/page.tsx"
      provides: "Blog listing page with ISR"
      contains: "revalidate"
    - path: "src/app/insights/[slug]/page.tsx"
      provides: "Individual blog post page with async params"
      contains: "await params"
  key_links:
    - from: "src/app/insights/page.tsx"
      to: "src/lib/sanity/client.ts"
      via: "Sanity client import for data fetching"
      pattern: "import.*sanityClient.*from.*@/lib/sanity/client"
    - from: "src/app/insights/[slug]/page.tsx"
      to: "src/components/blog/PortableTextRenderer.tsx"
      via: "Portable Text rendering of post body"
      pattern: "import.*PortableTextRenderer"
    - from: "src/components/sections/LatestInsights.tsx"
      to: "src/lib/sanity/queries.ts"
      via: "GROQ query for latest 3 posts"
      pattern: "latestPostsQuery"
---

<objective>
Build the blog/insights pages: PostCard component, PortableTextRenderer, /insights listing page, /insights/[slug] post page. Wire LatestInsights.tsx to Sanity and convert it to dark theme. Add /insights link to Nav and Footer.

Purpose: These are the primary deliverables for BLOG-01 and BLOG-02 — the user-facing blog experience.

Output: Complete blog UI ready to display posts once content is seeded in Sanity Studio.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-blog-launch/5-CONTEXT.md
@.planning/phases/05-blog-launch/05-RESEARCH.md
@.planning/phases/05-blog-launch/05-UI-SPEC.md
@.planning/phases/05-blog-launch/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — Sanity library contracts the executor needs -->

From src/lib/sanity/client.ts:
```typescript
export const sanityClient: SanityClient
```

From src/lib/sanity/types.ts:
```typescript
export interface Post {
  title: string
  slug: { current: string }
  tag: 'Technical' | 'Regulatory' | 'Comparison' | 'How-To'
  featuredImage?: { asset: { _ref: string } }
  readTime?: number
  publishedAt: string
  excerpt: string
  body?: unknown[]
}
```

From src/lib/sanity/queries.ts:
```typescript
export const allPostsQuery: string      // All published posts, ordered by date desc
export const postBySlugQuery: string     // Single post by slug.current == $slug
export const latestPostsQuery: string    // Latest 3 published posts
export const allPostSlugsQuery: string   // All slugs for generateStaticParams/sitemap
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostCard and PortableTextRenderer components</name>
  <files>
    src/components/sections/PostCard.tsx
    src/components/blog/PortableTextRenderer.tsx
  </files>
  <read_first>
    src/components/sections/LatestInsights.tsx
    src/lib/sanity/types.ts
    src/app/globals.css
    .planning/phases/05-blog-launch/05-UI-SPEC.md
    .planning/phases/05-blog-launch/05-RESEARCH.md
  </read_first>
  <action>
    1. Create `src/components/sections/PostCard.tsx` (Server Component, no 'use client'):
       - Import `Link` from `next/link` and `Post` from `@/lib/sanity/types`
       - Accept props: `post: Post`
       - Tag badge color map per UI-SPEC:
         - Technical: `bg-green-500/10 text-green-400 border-green-500/30`
         - Regulatory: `bg-amber-500/10 text-amber-400 border-amber-500/30`
         - Comparison: `bg-ink-700 text-ink-200 border-ink-600`
         - How-To: `bg-ink-700 text-ink-200 border-ink-600`
       - Card structure per UI-SPEC PostCard spec:
         - Outer: `<Link href={/insights/${post.slug.current}}>` with `className="group flex flex-col p-6 rounded-xl border border-ink-600 bg-ink-800 hover:border-green-400/50 hover:shadow-md transition-all duration-300"`
         - Tag badge: `inline-flex items-center px-2.5 py-1 rounded border text-xs tracking-wider mb-4 w-fit` with font-family JetBrains Mono, font-weight 400
         - Title: Barlow Condensed 16px weight 700, `text-ink-100 leading-snug group-hover:text-green-400 transition-colors mb-3`
         - Excerpt: Barlow 16px weight 400, `text-ink-200 leading-relaxed flex-1 mb-5`
         - Footer row: `border-t border-ink-700 pt-4 flex items-center justify-between`
           - Date: JetBrains Mono 12px weight 400, `text-ink-400` — format publishedAt as "Mar 2026" using `new Date(post.publishedAt).toLocaleDateString('en-US', { month: 'short', year: 'numeric' })`
           - Read time: JetBrains Mono 12px weight 400, `text-ink-400` — show `${post.readTime} min read` if readTime exists, else omit
           - "Read ->": JetBrains Mono 12px weight 400, `text-green-500 opacity-0 group-hover:opacity-100 transition-opacity`
       - Accessibility: full card is the link target (the outer Link wraps everything), aria-label on read time element: `aria-label="${post.readTime} minute read"`

    2. Create `src/components/blog/PortableTextRenderer.tsx` (Client Component, needs 'use client'):
       - Import `PortableText` and `PortableTextComponents` from `@portabletext/react`
       - Define `components: PortableTextComponents` object with mappings per UI-SPEC PortableTextRenderer Typography Map:
         - `block.normal`: `<p>` with `className="text-ink-100 leading-relaxed mb-6"` and `style={{ fontFamily: "'Barlow', sans-serif", fontSize: '16px' }}`
         - `block.h2`: `<h2>` with `className="text-ink-50 mt-12 mb-4"` and `style={{ fontFamily: "'Bebas Neue', sans-serif", fontSize: 'clamp(1.25rem, 2vw, 1.75rem)', lineHeight: '1.1' }}`
         - `block.h3`: `<h3>` with `className="text-ink-50 mt-8 mb-3"` and `style={{ fontFamily: "'Bebas Neue', sans-serif", fontSize: 'clamp(1.25rem, 2vw, 1.75rem)', lineHeight: '1.1' }}`
         - `block.blockquote`: `<blockquote>` with `className="border-l-4 border-amber-500 pl-6 italic text-ink-200 my-8"`
         - `marks.strong`: `<strong>` with `className="font-bold text-ink-50"` (Barlow weight 700)
         - `marks.em`: `<em>` with `className="italic text-ink-200"`
         - `marks.link`: `<a>` with `href={value?.href}` and `className="text-green-500 underline underline-offset-2 hover:text-green-400"` and `target={value?.href?.startsWith('http') ? '_blank' : undefined}` and `rel={value?.href?.startsWith('http') ? 'noopener noreferrer' : undefined}`
         - `list.bullet`: `<ul>` with `className="ml-6 mb-6 list-disc space-y-2"`
         - `list.number`: `<ol>` with `className="ml-6 mb-6 list-decimal space-y-2"`
         - `listItem.bullet`: `<li>` with `className="text-ink-100"` and `style={{ fontFamily: "'Barlow', sans-serif", fontSize: '16px' }}`
         - `listItem.number`: `<li>` with `className="text-ink-100"` and `style={{ fontFamily: "'Barlow', sans-serif", fontSize: '16px' }}`
         - `types.image`: `<figure>` with `className="my-8"` containing an `<img>` with `className="w-full rounded-lg object-cover"` (use @sanity/image-url to build the src from the asset ref, or fallback to empty string)
       - Export: `export function PortableTextRenderer({ value }: { value: unknown[] }) { return <PortableText value={value} components={components} /> }`
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && test -f src/components/sections/PostCard.tsx && test -f src/components/blog/PortableTextRenderer.tsx && grep -q "PostCard" src/components/sections/PostCard.tsx && grep -q "PortableTextRenderer" src/components/blog/PortableTextRenderer.tsx && echo "Components created"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/PostCard.tsx contains `export` and `PostCard`
    - src/components/sections/PostCard.tsx contains `bg-ink-800` (dark theme)
    - src/components/sections/PostCard.tsx contains `border-ink-600` (dark card border)
    - src/components/sections/PostCard.tsx contains `group-hover:text-green-400`
    - src/components/sections/PostCard.tsx contains `bg-green-500/10` (Technical tag color)
    - src/components/sections/PostCard.tsx contains `bg-amber-500/10` (Regulatory tag color)
    - src/components/sections/PostCard.tsx does NOT contain `bg-white` or `text-gray-900` (no light theme)
    - src/components/blog/PortableTextRenderer.tsx contains `'use client'`
    - src/components/blog/PortableTextRenderer.tsx contains `import { PortableText`
    - src/components/blog/PortableTextRenderer.tsx contains `border-amber-500` (blockquote)
    - src/components/blog/PortableTextRenderer.tsx contains `text-green-500` (link color)
    - src/components/blog/PortableTextRenderer.tsx contains `Bebas Neue` (heading font)
    - src/components/blog/PortableTextRenderer.tsx contains `Barlow` (body font)
  </acceptance_criteria>
  <done>PostCard component renders dark-themed blog cards with tag color map. PortableTextRenderer maps all Portable Text block types to site design tokens per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Create /insights listing page, /insights/[slug] post page, and wire LatestInsights + Nav + Footer</name>
  <files>
    src/app/insights/page.tsx
    src/app/insights/[slug]/page.tsx
    src/components/sections/LatestInsights.tsx
    src/components/layout/Nav.tsx
    src/components/layout/Footer.tsx
  </files>
  <read_first>
    src/components/sections/PostCard.tsx
    src/components/blog/PortableTextRenderer.tsx
    src/lib/sanity/client.ts
    src/lib/sanity/queries.ts
    src/lib/sanity/types.ts
    src/components/sections/LatestInsights.tsx
    src/components/layout/Nav.tsx
    src/components/layout/Footer.tsx
    src/app/globals.css
    .planning/phases/05-blog-launch/05-UI-SPEC.md
    .planning/phases/05-blog-launch/05-RESEARCH.md
    node_modules/next/dist/docs/01-app/02-guides/incremental-static-regeneration.md
  </read_first>
  <action>
    1. Create `src/app/insights/page.tsx` (Server Component, async):
       - Import `sanityClient` from `@/lib/sanity/client`, `allPostsQuery` from `@/lib/sanity/queries`, `Post` from `@/lib/sanity/types`, `PostCard` from `@/components/sections/PostCard`
       - Import `unstable_cache` from `next/cache`
       - Add `export const revalidate = 3600` (1hr ISR)
       - Add `export const metadata = { title: 'Insights | Santosh Petrochemical', description: 'Technical analysis, regulatory guides, and industry perspective from Santosh Petrochemical.' }`
       - Create cached fetch: `const getPosts = unstable_cache(async () => sanityClient.fetch<Post[]>(allPostsQuery), ['all-posts'], { revalidate: 3600 })`
       - Page layout per UI-SPEC /insights listing:
         - Hero section: `bg-ink-900 py-24`
           - Section label: `<p className="section-label mb-3">05 · INSIGHTS</p>`
           - H1: `className="text-ink-50"` with `style={{ fontFamily: "'Bebas Neue', sans-serif", fontSize: 'clamp(2.5rem, 5vw, 4rem)', lineHeight: '1.1' }}"` — text: "Insights from the Lab & Field"
           - Subtext: `className="text-ink-200 max-w-2xl mt-4"` with Barlow 16px weight 400 — text: "Technical analysis, regulatory guides, and industry perspective from Santosh Petrochemical."
         - Post grid section: `bg-ink-900 py-16`
           - `<div className="max-w-7xl mx-auto px-6 lg:px-8">`
           - If `posts.length === 0`: render empty state — H2 "No articles published yet." (Bebas Neue, ink-200) + P "Check back soon — our first insights are on the way." (Barlow, ink-400)
           - If posts exist: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">` mapping posts to `<PostCard key={post.slug.current} post={post} />`

    2. Create `src/app/insights/[slug]/page.tsx` (Server Component, async):
       - CRITICAL Next.js 16 pattern: `params` is a Promise — must declare `params: Promise<{ slug: string }>` and `const { slug } = await params`
       - Import `unstable_cache` from `next/cache`, `sanityClient` from client, queries, types, `PortableTextRenderer`, `notFound` from `next/navigation`
       - Add `export const revalidate = 3600`
       - Create cached fetch: `const getPost = unstable_cache(async (slug: string) => sanityClient.fetch<Post | null>(postBySlugQuery, { slug }), ['post'], { revalidate: 3600 })`
       - `generateStaticParams`: fetch allPostSlugsQuery, return `posts.map(p => ({ slug: p.slug }))`
       - `generateMetadata`: fetch post, return `{ title: post.title + ' | Santosh Insights' }`
       - If `!post`, call `notFound()`
       - Tag badge color map (same as PostCard): Technical=green, Regulatory=amber, Comparison/How-To=ink
       - Page layout per UI-SPEC /insights/[slug]:
         - Post Hero: `bg-ink-900 py-24`
           - `<div className="max-w-7xl mx-auto px-6 lg:px-8">`
           - Tag badge (same styling as PostCard)
           - Title: Bebas Neue `clamp(2.5rem, 5vw, 4rem)`, `text-ink-50 leading-none max-w-3xl mt-4`
           - Meta row: `className="flex items-center gap-4 mt-6 text-ink-400"` with JetBrains Mono 12px — Date formatted as "March 22, 2026" + separator dot + read time
           - Divider: `border-t border-ink-700 mt-8`
         - Post Body: `bg-ink-900 py-16`
           - `<article className="max-w-[65ch] mx-auto px-6">` containing `<PortableTextRenderer value={post.body ?? []} />`
         - Post Footer CTA: `bg-ink-800 py-16`
           - `<div className="max-w-7xl mx-auto px-6 lg:px-8 text-center">`
           - H2: "Interested in Group II+ RRBO?" — Bebas Neue, ink-100
           - Two buttons:
             - `<Link href="/sample-request">` with primary green button classes: `inline-flex px-6 py-3 rounded bg-green-500 text-white font-semibold hover:bg-green-400 transition-colors` — text "Request a Sample"
             - `<Link href="/contact">` with ghost button classes: `inline-flex px-6 py-3 rounded border border-ink-500 text-ink-200 hover:border-green-400 hover:text-green-400 transition-colors` — text "Get in Touch"

    3. Rewrite `src/components/sections/LatestInsights.tsx` (convert to async Server Component):
       - Remove the hardcoded `posts` array entirely
       - Import `sanityClient`, `latestPostsQuery`, `Post`, `PostCard`, `unstable_cache` from `next/cache`
       - Create cached fetch: `const getLatestPosts = unstable_cache(async () => sanityClient.fetch<Post[]>(latestPostsQuery), ['latest-posts'], { revalidate: 3600 })`
       - Change to `export default async function LatestInsights()`
       - Fetch posts: `const posts = await getLatestPosts()`
       - Convert ALL light-theme classes to dark ink-900 theme per D-07/D-08:
         - Section bg: `bg-white` -> `bg-ink-900`
         - Heading: `text-gray-900` -> `text-ink-50`
         - "All Articles" link: `text-green-600 hover:text-green-700` -> `text-green-500 hover:text-green-400`
       - Replace inline post card JSX with `<PostCard post={post} />` component
       - Keep the section label "08 . INSIGHTS" and "Latest from the lab & field" heading
       - If `posts.length === 0`, render the 3 placeholder cards with the original static data (graceful fallback before Sanity has content)

    4. Update `src/components/layout/Nav.tsx`:
       - Add `{ href: "/insights", label: "Insights" }` to the `navLinks` array — insert it after the "Quality" entry (position 7, last nav link before CTAs)

    5. Update `src/components/layout/Footer.tsx`:
       - Footer already has `{ label: "Insights", href: "/insights" }` in the Company links — verify it exists and no changes needed. If it does not exist, add it.
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && test -f src/app/insights/page.tsx && test -f "src/app/insights/[slug]/page.tsx" && grep -q "sanityClient" src/components/sections/LatestInsights.tsx && grep -q "insights" src/components/layout/Nav.tsx && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/insights/page.tsx contains `export const revalidate = 3600`
    - src/app/insights/page.tsx contains `unstable_cache`
    - src/app/insights/page.tsx contains `sanityClient.fetch`
    - src/app/insights/page.tsx contains `PostCard`
    - src/app/insights/page.tsx contains `05 · INSIGHTS`
    - src/app/insights/page.tsx contains `No articles published yet`
    - src/app/insights/page.tsx contains `bg-ink-900`
    - src/app/insights/page.tsx does NOT contain `bg-white`
    - src/app/insights/[slug]/page.tsx contains `await params`
    - src/app/insights/[slug]/page.tsx contains `Promise<{ slug: string }>`
    - src/app/insights/[slug]/page.tsx contains `PortableTextRenderer`
    - src/app/insights/[slug]/page.tsx contains `notFound`
    - src/app/insights/[slug]/page.tsx contains `generateStaticParams`
    - src/app/insights/[slug]/page.tsx contains `max-w-[65ch]`
    - src/app/insights/[slug]/page.tsx contains `Request a Sample`
    - src/components/sections/LatestInsights.tsx contains `sanityClient`
    - src/components/sections/LatestInsights.tsx contains `bg-ink-900`
    - src/components/sections/LatestInsights.tsx does NOT contain `bg-white`
    - src/components/sections/LatestInsights.tsx contains `PostCard`
    - src/components/layout/Nav.tsx contains `href: "/insights"`
    - src/components/layout/Footer.tsx contains `Insights`
  </acceptance_criteria>
  <done>/insights listing page renders post grid from Sanity with ISR. /insights/[slug] renders full post with Portable Text and footer CTA. LatestInsights wired to Sanity with dark theme. Nav and Footer include Insights link.</done>
</task>

</tasks>

<verification>
- `npm run build` passes (may warn about missing SANITY env vars but should not error)
- /insights page structure renders hero + grid
- /insights/[slug] page uses `await params` (Next.js 16 pattern)
- LatestInsights uses dark ink-900 theme, no light theme remnants
- PostCard and PortableTextRenderer use correct design tokens from UI-SPEC
- Nav has 7 links (Products, Process, Sustainability, Collect Used Oil, About, Quality, Insights)
</verification>

<success_criteria>
- Blog listing and post pages are complete Server Components with ISR caching
- All components follow dark ink-900 theme per D-08
- PortableTextRenderer maps all block types per UI-SPEC typography table
- Zero posts handled gracefully with empty state message
- Nav and Footer navigation updated with /insights link
</success_criteria>

<output>
After completion, create `.planning/phases/05-blog-launch/05-02-SUMMARY.md`
</output>
