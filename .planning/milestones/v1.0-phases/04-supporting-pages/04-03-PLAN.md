---
phase: 04-supporting-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sustainability/page.tsx
  - src/components/sections/sustainability/EPRProgressBars.tsx
  - src/components/sections/sustainability/CircularEconomySVG.tsx
autonomous: true
requirements: [SUST-01, SUST-02]
must_haves:
  truths:
    - "/sustainability page renders at correct URL with environmental opportunity framing"
    - "EPR progress bars animate on scroll showing 4 milestones (FY2025 5%, FY2027 15%, FY2029 30%, FY2031 50%)"
    - "Circular economy SVG renders 4-node loop with animated arrows"
    - "CO2 savings stat is cited from a real published source, not invented"
    - "IOCL MOU callout is present"
  artifacts:
    - path: "src/app/sustainability/page.tsx"
      provides: "Sustainability page Server Component composing client islands"
      min_lines: 80
    - path: "src/components/sections/sustainability/EPRProgressBars.tsx"
      provides: "Client component with IntersectionObserver + CSS transition animated bars"
      min_lines: 50
    - path: "src/components/sections/sustainability/CircularEconomySVG.tsx"
      provides: "Client component with Framer Motion animated SVG loop"
      min_lines: 60
  key_links:
    - from: "src/app/sustainability/page.tsx"
      to: "EPRProgressBars"
      via: "import and render"
      pattern: "import EPRProgressBars"
    - from: "src/app/sustainability/page.tsx"
      to: "CircularEconomySVG"
      via: "import and render"
      pattern: "import CircularEconomySVG"
    - from: "src/components/sections/sustainability/CircularEconomySVG.tsx"
      to: "framer-motion"
      via: "motion.path animate"
      pattern: "motion\\.path"
---

<objective>
Build the /sustainability page with animated EPR progress bars, circular economy SVG loop, and research-based CO2 savings stat.

Purpose: Deep-dive sustainability page expanding on the homepage SustainabilitySnapshot. Communicates the EPR mandate progression, environmental impact of re-refining, and circular economy model to compliance teams and investors. The most technically complex page in Phase 4 due to two interactive client components.

Output: `src/app/sustainability/page.tsx` (Server Component), `EPRProgressBars.tsx` (client), `CircularEconomySVG.tsx` (client).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supporting-pages/4-CONTEXT.md
@.planning/phases/04-supporting-pages/04-UI-SPEC.md
@.planning/phases/04-supporting-pages/04-RESEARCH.md

@src/components/sections/SustainabilitySnapshot.tsx
@src/app/globals.css

<interfaces>
<!-- SustainabilitySnapshot.tsx — EPR bar pattern to replicate (lines 16-23, 119-153) -->
const eprMilestones = [
  { year: "FY2025", pct: 5, label: "EPR mandate begins" },
  { year: "FY2027", pct: 15, label: "Growing momentum" },
  { year: "FY2029", pct: 30, label: "Formal sector scales" },
  { year: "FY2031", pct: 50, label: "National target" },
];

// IntersectionObserver pattern:
const ref = useRef<HTMLDivElement>(null);
const [visible, setVisible] = useState(false);
useEffect(() => {
  const observer = new IntersectionObserver(
    ([e]) => { if (e.isIntersecting) { setVisible(true); observer.disconnect(); } },
    { threshold: 0.2 }
  );
  if (ref.current) observer.observe(ref.current);
  return () => observer.disconnect();
}, []);

<!-- Framer Motion — already installed (^12.38.0) -->
import { motion } from "framer-motion";
<!-- motion.path for SVG stroke animation -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EPRProgressBars client component and CircularEconomySVG client component</name>
  <files>src/components/sections/sustainability/EPRProgressBars.tsx, src/components/sections/sustainability/CircularEconomySVG.tsx</files>
  <read_first>
    - src/components/sections/SustainabilitySnapshot.tsx (COPY the IntersectionObserver + CSS transition pattern for progress bars; reference the SVG structure for circular economy)
    - src/app/globals.css (design tokens)
    - .planning/phases/04-supporting-pages/04-UI-SPEC.md (EPRProgressBars spec, CircularEconomySVG spec)
    - .planning/phases/04-supporting-pages/04-RESEARCH.md (Pattern 3 and Pattern 4 code examples)
  </read_first>
  <action>
**Create directory:** `src/components/sections/sustainability/` (if it does not exist).

**File 1: `src/components/sections/sustainability/EPRProgressBars.tsx`**

`"use client"` component. Architecture: IntersectionObserver (threshold 0.2) + CSS transition — SAME pattern as SustainabilitySnapshot.tsx. Do NOT use Framer Motion here (per UI-SPEC and RESEARCH).

Structure:
- Wrap in a `<section className="bg-ink-900 py-24">` with `max-w-7xl mx-auto px-6 lg:px-8`
- 2-column grid on desktop: `grid lg:grid-cols-2 gap-16 items-center`
- Left column: narrative text
  - Section label: `<p className="section-label mb-3">01 · EPR MANDATE</p>`
  - Heading (Bebas Neue, clamp 1.75rem-3rem): "A Mandate With a Deadline"
  - Body paragraph (Barlow 300, ink-200): Explain EPR regulations under Hazardous Waste Rules 2023, mandate for lubricant manufacturers to blend increasing RRBO proportions, positioning Santosh to supply the Group II+ segment.
- Right column: progress bars (ref attached here)
  - Label above bars: `<h3 className="text-sm text-ink-200 mb-8" style={{ fontFamily: "'JetBrains Mono', monospace", letterSpacing: "0.1em" }}>EPR RRBO BLENDING MANDATE PROGRESSION</h3>`
  - 4 milestone bars in `space-y-5`:
    - Data: `[{ year: "FY2025", pct: 5, label: "EPR mandate begins" }, { year: "FY2027", pct: 15, label: "Growing momentum" }, { year: "FY2029", pct: 30, label: "Formal sector scales" }, { year: "FY2031", pct: 50, label: "National target" }]`
    - Each bar row: year label (JetBrains Mono 14px ink-200) left, percentage value (Bebas Neue 24px amber-500) + label right
    - Bar track: `h-3 bg-ink-700 rounded-full overflow-hidden`
    - Bar fill: `h-full rounded-full transition-all duration-1000` with conditional class — final bar (FY2031) uses `bg-gradient-to-r from-green-600 to-green-400`, others use `bg-green-700`
    - Width: `style={{ width: visible ? \`${m.pct * 2}%\` : "0%", transitionDelay: \`${i * 0.2}s\` }}`
  - Background accent glow: `radial-gradient(ellipse 70% 50% at 80% 50%, rgba(58,158,100,0.07) 0%, transparent 70%)`

IntersectionObserver pattern — copy exactly from SustainabilitySnapshot.tsx lines 16-23:
```typescript
const ref = useRef<HTMLDivElement>(null);
const [visible, setVisible] = useState(false);
useEffect(() => {
  const observer = new IntersectionObserver(
    ([e]) => { if (e.isIntersecting) { setVisible(true); observer.disconnect(); } },
    { threshold: 0.2 }
  );
  if (ref.current) observer.observe(ref.current);
  return () => observer.disconnect();
}, []);
```

**File 2: `src/components/sections/sustainability/CircularEconomySVG.tsx`**

`"use client"` component using Framer Motion for the arrow loop animation. Node boxes appear on scroll via IntersectionObserver + CSS opacity/translateY transition.

Structure:
- Wrap in `<section className="bg-ink-800 py-24">` with `max-w-7xl mx-auto px-6 lg:px-8`
- Section label: `<p className="section-label mb-3">02 · CIRCULAR ECONOMY</p>`
- Heading (Bebas Neue): "The Loop That Closes the Gap"
- SVG: `viewBox="0 0 400 400"` with `className="w-full max-w-lg mx-auto"`
- 4 nodes in circular/diamond layout:
  - Top: "Used Oil Collected" (x:150, y:30)
  - Right: "Re-Refined at Santosh" (x:270, y:160)
  - Bottom: "Back Into Engines" (x:150, y:290)
  - Left: "Collected Again" (x:30, y:160)
- Node box styling: `fill="#1c1e18" stroke="#2d7a4e" strokeWidth="1.5"` with `rx="8"`, width 100, height 50
- Text inside nodes: `fontFamily="Barlow Condensed, sans-serif"` `fontSize="10"` `fill="#d4d8c8"` `fontWeight="600"`
- Forward arrows (green): `stroke="#3a9e64" strokeWidth="2"` connecting nodes clockwise
- Use `<defs><marker>` for arrowheads (same pattern as SustainabilitySnapshot.tsx SVG)
- **Animated return/loop path:** Use `<motion.path>` for the connecting arrows with:
  ```typescript
  <motion.path
    d="..." // clockwise path connecting all 4 nodes
    fill="none"
    stroke="#3a9e64"
    strokeWidth="2"
    strokeDasharray="8 4"
    animate={{ strokeDashoffset: [0, -48] }}
    transition={{ repeat: Infinity, duration: 3, ease: "linear" }}
  />
  ```
- Return arc (amber dashed): `stroke="#f59e0b" strokeDasharray="4 3"` — a dashed path showing the "collection" return leg
- Node reveal: IntersectionObserver + CSS `opacity 0->1, translateY 15px->0` with `transition: opacity 0.5s ease ${i * 0.1}s, transform 0.5s ease ${i * 0.1}s` (same as SustainabilitySnapshot stat cards)

Import from framer-motion: `{ motion }` only. Do not import useScroll/useTransform (not needed here).
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && ls src/components/sections/sustainability/EPRProgressBars.tsx src/components/sections/sustainability/CircularEconomySVG.tsx && grep -c "use client" src/components/sections/sustainability/EPRProgressBars.tsx src/components/sections/sustainability/CircularEconomySVG.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/sustainability/EPRProgressBars.tsx exists and contains "use client"
    - EPRProgressBars.tsx contains "IntersectionObserver" (NOT "useScroll" or Framer Motion)
    - EPRProgressBars.tsx contains "FY2025" and "FY2027" and "FY2029" and "FY2031"
    - EPRProgressBars.tsx contains "transition-all duration-1000"
    - EPRProgressBars.tsx contains "bg-gradient-to-r from-green-600 to-green-400" (final bar)
    - EPRProgressBars.tsx contains "01 · EPR MANDATE"
    - src/components/sections/sustainability/CircularEconomySVG.tsx exists and contains "use client"
    - CircularEconomySVG.tsx contains `import { motion } from "framer-motion"` or equivalent
    - CircularEconomySVG.tsx contains "motion.path"
    - CircularEconomySVG.tsx contains "strokeDashoffset"
    - CircularEconomySVG.tsx contains "repeat: Infinity" or "Infinity"
    - CircularEconomySVG.tsx contains "Used Oil Collected" and "Re-Refined" and "Back Into Engines" and "Collected Again"
    - CircularEconomySVG.tsx contains "02 · CIRCULAR ECONOMY"
    - CircularEconomySVG.tsx contains "#f59e0b" (amber return arc)
  </acceptance_criteria>
  <done>
    EPRProgressBars component animates 4 milestone bars on scroll. CircularEconomySVG renders 4-node loop with Framer Motion animated arrows and amber return arc.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /sustainability page composing EPR bars, circular economy SVG, CO2 stat, and IOCL MOU callout</name>
  <files>src/app/sustainability/page.tsx</files>
  <read_first>
    - src/components/sections/sustainability/EPRProgressBars.tsx (just created — verify exports)
    - src/components/sections/sustainability/CircularEconomySVG.tsx (just created — verify exports)
    - src/components/sections/SustainabilitySnapshot.tsx (IOCL MOU callout pattern — lines 91-106)
    - src/app/sample-request/page.tsx (page shell pattern)
    - src/app/globals.css (design tokens)
    - .planning/phases/04-supporting-pages/04-UI-SPEC.md (visual contract for /sustainability)
    - .planning/phases/04-supporting-pages/4-CONTEXT.md (decisions D-01 through D-04)
  </read_first>
  <action>
Create `src/app/sustainability/page.tsx` as a Server Component (NO "use client" directive). This page composes the two client island components with static content sections.

**Metadata:**
```typescript
export const metadata: Metadata = {
  title: "Sustainability | Santosh Petrochemical",
  description: "Turning India's used oil problem into an opportunity. EPR mandate progression, CO₂ savings, and circular economy — Santosh is building the infrastructure for change.",
};
```

**Hero section** (`bg-ink-950 py-24 lg:py-32 pt-36 lg:pt-40`, per D-04 environmental opportunity framing):
- Add `gradient-mesh` overlay: `radial-gradient(ellipse 70% 50% at 30% 50%, rgba(58,158,100,0.1) 0%, transparent 70%)`
- Section label: (no numbered label on hero — just the heading)
- Headline (Bebas Neue, clamp 1.75rem-3rem): "Turning India's Used Oil Problem Into an Opportunity"
- Subhead (Barlow 300, ink-200): "India generates 1.3 million tonnes of used oil annually. Less than 15% is formally recycled. Santosh is building the infrastructure to change that."

**EPR Progress Bars section:** Simply render `<EPRProgressBars />` — it is self-contained with its own section wrapper, label, and background.

**Circular Economy SVG section:** Render `<CircularEconomySVG />` — also self-contained.

**Section 3: Environmental Impact / CO2 Savings** (`bg-ink-800/50 py-24`):
- Section label: `<p className="section-label mb-3">03 · ENVIRONMENTAL IMPACT</p>`
- Heading (Bebas Neue): "Less Energy. Less Carbon. Less Waste."
- CO2 savings stat block (per D-02 — research-based, cited):
  - **IMPORTANT: The executor MUST research "CO2 savings re-refined base oil vs virgin lifecycle analysis" before writing this section.** Search for publications from GEIR (Group for the Reuse of Lubricants), ATIEL, or published LCA studies. Known anchor: the project DPR cites "66% less energy vs virgin oil production."
  - If a specific CO2/tonne figure is found, present it as: amber value (Bebas Neue 2xl) + Barlow label + source citation in `<p className="text-xs text-ink-400 mt-2" style={{ fontFamily: "'JetBrains Mono', monospace" }}>`
  - If only the energy figure is available, present: "66%" value, "Less energy consumed in production vs virgin base oil" label, source: "Source: TEV Report / DPR — Santosh Petrochemical Innovations"
  - Stat card pattern: `p-6 rounded-xl border border-ink-600/40 bg-ink-800 max-w-md`
  - Additional context paragraph about zero waste to landfill (asphalt flux residue reused in road construction).

**IOCL MOU callout** (reuse exact visual pattern from SustainabilitySnapshot.tsx lines 91-106):
```html
<div className="p-5 rounded-xl border border-green-500/30 bg-green-900/10 flex gap-4 mt-12 max-w-2xl">
  <div className="w-1 rounded-full bg-green-500 flex-shrink-0" />
  <div>
    <div className="text-xs text-green-400 mb-1" style={{ fontFamily: "'JetBrains Mono', monospace" }}>
      MARCH 2026 · MOU SIGNED
    </div>
    <p className="text-sm text-ink-100 leading-relaxed" style={{ fontFamily: "'Barlow', sans-serif" }}>
      IOCL and Re Sustainability signed an MOU to build India's first national Group II+ RRBO circular economy — directly validating Santosh's technology and market positioning.
    </p>
  </div>
</div>
```

**Imports:** `type { Metadata }`, `Nav`, `Footer`, `WhatsAppButton`, `EPRProgressBars`, `CircularEconomySVG`.
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/sustainability/page.tsx exists and does NOT contain "use client"
    - File contains `export const metadata` with title "Sustainability | Santosh Petrochemical"
    - File contains "Turning India's Used Oil Problem Into an Opportunity" (hero headline per D-04)
    - File contains "1.3 million tonnes" (hero subhead)
    - File contains `import EPRProgressBars` and `import CircularEconomySVG`
    - File contains `<EPRProgressBars` and `<CircularEconomySVG`
    - File contains "03 · ENVIRONMENTAL IMPACT"
    - File contains "MARCH 2026 · MOU SIGNED" (IOCL callout)
    - File contains a CO2/energy savings stat with a source citation (grep for "Source:" or "source:")
    - File contains "66%" (at minimum the energy figure — if CO2 per tonne found, that too)
    - File does NOT contain fabricated CO2 per tonne numbers without a cited source
    - Build succeeds without errors
  </acceptance_criteria>
  <done>
    /sustainability page renders with hero (environmental opportunity framing), animated EPR progress bars (4 milestones), circular economy SVG loop (4 animated nodes), research-cited CO2/energy savings stat, and IOCL MOU callout.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- Navigate to `/sustainability` — page renders all sections
- EPR progress bars animate when scrolled into view
- Circular economy SVG shows animated dashed arrows looping
- CO2 savings stat has a visible source citation
- IOCL MOU callout renders with green border
- No console errors or hydration mismatches
</verification>

<success_criteria>
- /sustainability page accessible at correct URL
- EPR progress bars animate (FY2025 5%, FY2027 15%, FY2029 30%, FY2031 50%)
- Circular economy SVG renders 4-node loop with continuous arrow animation
- CO2/energy savings stat is cited from a real source (not invented)
- IOCL MOU callout present
- Page follows Server Component + client island pattern
</success_criteria>

<output>
After completion, create `.planning/phases/04-supporting-pages/04-03-SUMMARY.md`
</output>
