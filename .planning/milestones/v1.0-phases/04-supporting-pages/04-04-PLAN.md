---
phase: 04-supporting-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/collect/page.tsx
  - src/components/sections/collect/PickupForm.tsx
  - src/app/api/collect-pickup/route.ts
autonomous: true
requirements: [COLL-01, COLL-02]
user_setup:
  - service: google-sheets
    why: "Pickup form data storage"
    env_vars: []
    dashboard_config:
      - task: "Create 'Collect Pickup' tab in existing spreadsheet (ID: 1oc5WnrHE_MA139bN5X7PF6bEYZCbkNJz8LA5LlO09vI)"
        location: "Google Sheets → open spreadsheet → click '+' tab at bottom → rename to 'Collect Pickup'"
must_haves:
  truths:
    - "/collect page renders at correct URL with warm/accessible hero tone"
    - "How it works section shows 3-4 process steps"
    - "Service area lists all 7 cities as text (no map)"
    - "Pickup form validates business name and phone as required"
    - "Form success state shows WhatsApp link (wa.me/919810121438)"
    - "Page-specific WhatsApp CTA block is distinct from floating button"
    - "Form POSTs to /api/collect-pickup which writes to Google Sheets"
  artifacts:
    - path: "src/app/collect/page.tsx"
      provides: "Collection page Server Component with static sections"
      min_lines: 100
    - path: "src/components/sections/collect/PickupForm.tsx"
      provides: "Client component with form state, validation, Sheets POST, success state"
      min_lines: 80
    - path: "src/app/api/collect-pickup/route.ts"
      provides: "Google Sheets API route for pickup form data"
      min_lines: 30
  key_links:
    - from: "src/components/sections/collect/PickupForm.tsx"
      to: "/api/collect-pickup"
      via: "fetch POST"
      pattern: 'fetch.*api/collect-pickup'
    - from: "src/app/api/collect-pickup/route.ts"
      to: "Google Sheets API"
      via: "GoogleAuth + sheets append"
      pattern: "sheets.googleapis.com"
---

<objective>
Build the /collect page with pickup scheduling form, service area list, process steps, and WhatsApp CTA.

Purpose: Conversion page for used oil suppliers (workshop owners, fleet operators). Uses warm/accessible language to encourage pickups. Form submits to Google Sheets via new API route. WhatsApp CTA provides instant contact channel distinct from the global floating button.

Output: `src/app/collect/page.tsx` (Server Component), `PickupForm.tsx` (client), `/api/collect-pickup/route.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supporting-pages/4-CONTEXT.md
@.planning/phases/04-supporting-pages/04-UI-SPEC.md
@.planning/phases/04-supporting-pages/04-RESEARCH.md

@src/components/ui/SampleRequestForm.tsx
@src/app/api/sample-request/route.ts
@src/app/sample-request/page.tsx
@src/app/globals.css

<interfaces>
<!-- SampleRequestForm.tsx — form pattern to replicate -->
"use client";
import { useState } from "react";
// useState for form fields + errors + submitted
// handleSubmit: e.preventDefault(), validate required fields, setErrors, fetch POST, setSubmitted
// Success state: green checkmark circle + heading + WhatsApp link

<!-- sample-request/route.ts — API route pattern to replicate -->
import { NextRequest } from "next/server";
import { GoogleAuth } from "google-auth-library";
const SPREADSHEET_ID = "1oc5WnrHE_MA139bN5X7PF6bEYZCbkNJz8LA5LlO09vI";
// Same SPREADSHEET_ID, new SHEET_NAME = "Collect Pickup"
// GoogleAuth with GOOGLE_CLIENT_EMAIL + GOOGLE_PRIVATE_KEY env vars
// Graceful degradation: Sheets failure returns 200 with warning

<!-- Input class pattern (exact from SampleRequestForm) -->
className="w-full bg-ink-900/80 border border-ink-600/60 rounded-lg px-4 py-3 text-sm text-ink-100 placeholder-ink-400 focus:border-green-500/60 focus:outline-none transition-colors"
style={{ fontFamily: "'Barlow', sans-serif" }}

<!-- Label class pattern -->
className="block text-xs text-ink-200 mb-1.5 font-mono tracking-wider"

<!-- Error message pattern -->
className="mt-1 text-xs text-red-400" style={{ fontFamily: "'Barlow', sans-serif" }}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/collect-pickup route and PickupForm client component</name>
  <files>src/app/api/collect-pickup/route.ts, src/components/sections/collect/PickupForm.tsx</files>
  <read_first>
    - src/app/api/sample-request/route.ts (COPY this file and modify — exact same structure)
    - src/components/ui/SampleRequestForm.tsx (form pattern: useState, validation, fetch POST, success state)
    - src/app/globals.css (design tokens)
    - .planning/phases/04-supporting-pages/04-UI-SPEC.md (PickupForm spec — field list, validation, success state, input classes)
    - .planning/phases/04-supporting-pages/4-CONTEXT.md (decisions D-05, D-06, D-08, D-09)
  </read_first>
  <action>
**Create directories:** `src/app/api/collect-pickup/` and `src/components/sections/collect/` (if they do not exist).

**File 1: `src/app/api/collect-pickup/route.ts`**

Copy `src/app/api/sample-request/route.ts` exactly and make these changes:
- `const SHEET_NAME = "Collect Pickup";` (was "Sample Request")
- Destructure: `const { business, phone, location, quantity, frequency } = body;` (was name, address, phone, email, grade, quantity, application)
- Row: `const row = [timestamp, business, phone, location ?? "", quantity ?? "", frequency ?? ""];`
- Error log prefix: `"collect-pickup route error:"` (was "sample-request route error:")

Everything else stays identical: same SPREADSHEET_ID (`"1oc5WnrHE_MA139bN5X7PF6bEYZCbkNJz8LA5LlO09vI"`), same GoogleAuth pattern, same graceful degradation (200 with warning on Sheets failure), same env var names (GOOGLE_CLIENT_EMAIL, GOOGLE_PRIVATE_KEY).

**File 2: `src/components/sections/collect/PickupForm.tsx`**

`"use client"` component. Mirror SampleRequestForm.tsx structure exactly, with these field changes per D-06:

State:
```typescript
const [submitted, setSubmitted] = useState(false);
const [submitting, setSubmitting] = useState(false);
const [errors, setErrors] = useState<{ business?: string; phone?: string }>({});
const [form, setForm] = useState({
  business: "", phone: "", location: "", quantity: "", frequency: "",
});
```

Frequency options:
```typescript
const frequencies = ["One-time pickup", "Regular (weekly)", "Regular (monthly)", "Not sure yet"];
```

Validation (per D-06 — Business name and Phone required):
```typescript
const newErrors: { business?: string; phone?: string } = {};
if (!form.business.trim()) newErrors.business = "Business name is required";
if (!form.phone.trim()) newErrors.phone = "Phone number is required";
```

Form fields (5 fields, in this order):
1. **Business Name** * — text input, required, placeholder "Sharma Motors", id "cp-business"
2. **Phone Number** * — tel input, required, placeholder "+91 98101 21438", id "cp-phone"
3. **Location / Address** — text input, optional, placeholder "Workshop address or area", id "cp-location"
4. **Quantity Available** — text input, optional, placeholder "e.g. 200 litres, 4 drums", id "cp-quantity"
5. **Preferred Frequency** — select dropdown, optional, id "cp-frequency", options: ["", ...frequencies], first option label "Select frequency..."

All inputs use exact same classes as SampleRequestForm:
- Input: `className="w-full bg-ink-900/80 border border-ink-600/60 rounded-lg px-4 py-3 text-sm text-ink-100 placeholder-ink-400 focus:border-green-500/60 focus:outline-none transition-colors" style={{ fontFamily: "'Barlow', sans-serif" }}`
- Label: `className="block text-xs text-ink-200 mb-1.5 font-mono tracking-wider"`
- Error: `<p className="mt-1 text-xs text-red-400" style={{ fontFamily: "'Barlow', sans-serif" }}>{errors.field}</p>`

Submit button: Full-width, `className="w-full py-3.5 bg-green-500 text-white rounded-lg font-semibold hover:bg-green-400 transition-colors duration-200 disabled:opacity-70 disabled:cursor-not-allowed"`, text: "Schedule Pickup →", disabled while submitting.

POST target: `fetch("/api/collect-pickup", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) })`

**Success state** (per D-05, D-09):
- Green checkmark circle (same SVG as SampleRequestForm success state)
- Heading (Barlow Condensed bold): "Pickup Scheduled"
- Body (Barlow sm ink-200): "We'll be in touch within 24 hours to confirm your pickup window."
- WhatsApp CTA link: `<a href="https://wa.me/919810121438" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 mt-4 px-6 py-3 bg-green-500 text-white rounded-lg font-semibold hover:bg-green-400 transition-colors" style={{ fontFamily: "'Barlow Condensed', sans-serif" }}>Chat on WhatsApp →</a>`

Form wrapper: `<form onSubmit={handleSubmit} className="space-y-5" noValidate>`
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && ls src/app/api/collect-pickup/route.ts src/components/sections/collect/PickupForm.tsx && grep -c "Collect Pickup" src/app/api/collect-pickup/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/collect-pickup/route.ts exists
    - route.ts contains `const SHEET_NAME = "Collect Pickup"`
    - route.ts contains `const SPREADSHEET_ID = "1oc5WnrHE_MA139bN5X7PF6bEYZCbkNJz8LA5LlO09vI"`
    - route.ts contains `business, phone, location, quantity, frequency`
    - route.ts contains `GoogleAuth` import
    - route.ts contains graceful degradation: `warning: "Sheets write failed"` or similar
    - src/components/sections/collect/PickupForm.tsx exists and contains "use client"
    - PickupForm.tsx contains `fetch("/api/collect-pickup"`
    - PickupForm.tsx contains "Business name is required" and "Phone number is required" (validation messages)
    - PickupForm.tsx contains "One-time pickup" and "Regular (weekly)" (frequency options)
    - PickupForm.tsx contains "wa.me/919810121438" (WhatsApp link in success state)
    - PickupForm.tsx contains "Schedule Pickup" (submit button text)
    - PickupForm.tsx contains "Pickup Scheduled" (success heading)
    - PickupForm.tsx contains `noValidate` on form element
    - PickupForm.tsx uses exact input classes: "bg-ink-900/80 border border-ink-600/60 rounded-lg"
  </acceptance_criteria>
  <done>
    API route writes pickup data to "Collect Pickup" Google Sheets tab with graceful degradation. PickupForm validates business name + phone, POSTs to /api/collect-pickup, shows success state with WhatsApp link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /collect page with hero, process steps, service area, form, and WhatsApp CTA block</name>
  <files>src/app/collect/page.tsx</files>
  <read_first>
    - src/components/sections/collect/PickupForm.tsx (just created — verify export name)
    - src/app/sample-request/page.tsx (page shell pattern: 2-column layout with form card)
    - src/app/globals.css (design tokens)
    - .planning/phases/04-supporting-pages/04-UI-SPEC.md (visual contract for /collect — hero, process steps, service area, WhatsApp CTA block)
    - .planning/phases/04-supporting-pages/4-CONTEXT.md (decisions D-05 through D-09)
  </read_first>
  <action>
Create `src/app/collect/page.tsx` as a Server Component (NO "use client" directive).

**Metadata:**
```typescript
export const metadata: Metadata = {
  title: "Used Oil Collection | Santosh Petrochemical",
  description: "We pick up your used engine oil. Free collection across Delhi NCR, Ghaziabad, Meerut, and Western UP. Schedule a pickup or WhatsApp us.",
};
```

**Hero section** (`bg-ink-950 py-24 lg:py-32 pt-36 lg:pt-40`, per D-08 warm tone):
- Headline (Bebas Neue, clamp): "We pick up. You get paid. No hassle."
- Subhead (Barlow 300, ink-200): "Turn your used engine oil into income. We handle collection, logistics, and payment. You focus on your business."

**Section 1: How It Works** (`bg-ink-900 py-24`):
- Section label: `<p className="section-label mb-3">01 · HOW IT WORKS</p>`
- Heading (Bebas Neue): "Three Simple Steps"
- 3 step cards in horizontal row on desktop: `grid lg:grid-cols-3 gap-6 mt-10`
- Each card: `bg-ink-800 rounded-xl border border-ink-600/40 p-6`
  - Step number: `<span className="text-xs text-amber-500 mb-2 block" style={{ fontFamily: "'JetBrains Mono', monospace" }}>STEP 01</span>`
  - Title: Barlow Condensed 18px semibold white
  - Description: Barlow 14px ink-200

Step data:
1. Title: "Schedule a Pickup", Description: "Fill out the form below or message us on WhatsApp. Tell us your location and approximate quantity."
2. Title: "We Collect", Description: "Our team arrives at your location with the right equipment. We handle all logistics and paperwork."
3. Title: "You Get Paid", Description: "Receive payment for your used oil. Regular suppliers get priority scheduling and better rates."

**Section 2: Service Area** (`bg-ink-800 py-24`, per D-07 text list):
- Section label: `<p className="section-label mb-3">02 · SERVICE AREAS</p>`
- Card wrapper: `bg-ink-800 rounded-xl border border-ink-600/40 p-6 max-w-2xl` (or inline)
- Heading (Barlow Condensed semibold white): "Currently Serving"
- 7 cities listed, each with a green dot: `<div className="flex items-center gap-3 py-2"><div className="w-2 h-2 rounded-full bg-green-500 flex-shrink-0" /><span className="text-ink-100" style={{ fontFamily: "'Barlow', sans-serif", fontSize: "0.9375rem" }}>{city}</span></div>`
- Cities: Delhi NCR, Ghaziabad, Meerut, Muzaffarnagar, Saharanpur, Moradabad, Rampur
- Below list: `<p className="text-sm text-ink-400 mt-4" style={{ fontFamily: "'Barlow', sans-serif" }}>Expanding to additional locations across Uttar Pradesh. Contact us for coverage in your area.</p>`

**Section 3: Pickup Form** (`bg-ink-900 py-24`):
- 2-column grid: `grid lg:grid-cols-2 gap-16 items-start`
- Left column: contextual copy
  - Heading (Bebas Neue): "Schedule a Pickup"
  - Body paragraph about free collection, no minimum quantity, flexible scheduling
  - Bullet points: "Free collection — no pickup fees", "Flexible scheduling — one-time or regular", "Prompt payment — transparent pricing", "All paperwork handled by us"
  - Each bullet: `<div className="flex items-start gap-3"><div className="w-1.5 h-1.5 rounded-full bg-green-400 mt-2 flex-shrink-0" /><span className="text-sm text-ink-200" style={{ fontFamily: "'Barlow', sans-serif" }}>{text}</span></div>`
- Right column: form card
  - `<div className="bg-ink-800/80 border border-ink-600/50 rounded-2xl p-8"><PickupForm /></div>`

**WhatsApp CTA Block** (per D-09 — page-specific, distinct from floating button):
- Full-width section: `bg-green-900/20 py-16`
- Inner container: `max-w-2xl mx-auto px-6 lg:px-8 text-center`
- Heading (Barlow Condensed 24px semibold white): "Prefer to chat?"
- Body (Barlow sm ink-200): "Message us directly on WhatsApp for a faster response."
- WhatsApp button: `<a href="https://wa.me/919810121438" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-3 mt-6 px-8 py-4 bg-green-500 text-white rounded-xl font-semibold hover:bg-green-400 transition-colors" style={{ fontFamily: "'Barlow Condensed', sans-serif", fontSize: "1.25rem" }}>` with WhatsApp SVG icon (simple path: `M17.472 14.382c-.297-.149-1.758-.867-2.03-.967...` — use a minimal WhatsApp icon SVG inline, or use the text-only approach: "Chat on WhatsApp →")
- Button text: "Chat on WhatsApp →"
- This block sits between the form section and the Footer.

**Imports:** `type { Metadata }`, `Nav`, `Footer`, `WhatsAppButton`, `PickupForm`.
  </action>
  <verify>
    <automated>cd /Users/abindal/dev/SantoshWebsite && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/collect/page.tsx exists and does NOT contain "use client"
    - File contains `export const metadata` with title "Used Oil Collection | Santosh Petrochemical"
    - File contains "We pick up. You get paid. No hassle." (hero headline per D-08)
    - File contains "01 · HOW IT WORKS" and "02 · SERVICE AREAS"
    - File contains all 7 cities: "Delhi NCR", "Ghaziabad", "Meerut", "Muzaffarnagar", "Saharanpur", "Moradabad", "Rampur"
    - File contains "STEP 01" or "Step 01" (process steps)
    - File contains `import PickupForm` and `<PickupForm`
    - File contains "wa.me/919810121438" (page-specific WhatsApp CTA block)
    - File contains "Prefer to chat?" (WhatsApp CTA heading)
    - File contains "Chat on WhatsApp" (WhatsApp button text)
    - File contains `bg-green-900/20` (WhatsApp CTA block background)
    - File imports Nav, Footer, WhatsAppButton
    - Build succeeds without errors
  </acceptance_criteria>
  <done>
    /collect page renders with warm hero, 3 process steps, service area text list (7 cities), pickup form in 2-column layout, and prominent page-specific WhatsApp CTA block above footer.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- Navigate to `/collect` — page renders all sections
- Fill form with valid data — success state appears with WhatsApp link
- Leave business name empty — validation error "Business name is required" shows
- Leave phone empty — validation error "Phone number is required" shows
- Service area lists all 7 cities
- WhatsApp CTA block visible as distinct section above footer
- No console errors
</verification>

<success_criteria>
- /collect page accessible at correct URL
- Hero uses warm/accessible tone ("We pick up. You get paid. No hassle.")
- 3 process steps rendered
- Service area lists 7 cities as text (no map)
- Pickup form validates business name + phone as required
- Form POSTs to /api/collect-pickup
- Success state shows WhatsApp link
- Page-specific WhatsApp CTA block is visually distinct from floating button
- API route mirrors sample-request pattern with "Collect Pickup" sheet tab
</success_criteria>

<output>
After completion, create `.planning/phases/04-supporting-pages/04-04-SUMMARY.md`
</output>
